# voice-intent-pipeline
End-to-end slot extraction pipeline for food/restaurant voice commands — transformer encoder built from scratch in PyTorch, trained on LLM-generated data, runs on Apple Silicon (MPS).
